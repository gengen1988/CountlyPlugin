CountlyPlugin
=============

Countly Cordova Plugin


## 安装方法

```
cordova plugin add https://github.com/gengen1988/CountlyPlugin.git --variable KEY={YOUR_APP_KEY} --variable SERVER={YOUR_SERVER_URL}
```

## 使用方法


```
countly(key, [count, sum]);
```

```
countly(key, [segment, count, sum]);
```
